<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seekisle.adminservice.user.mapper.AppUserMapper">


    <select id="selectByPhoneNumber" parameterType="java.lang.String" resultType="com.seekisle.adminservice.user.domain.entity.AppUser">
        select * from app_user where phone_number = #{phoneNumber}
    </select>

    <select id="selectByOpenId" parameterType="java.lang.String" resultType="com.seekisle.adminservice.user.domain.entity.AppUser">
        select * from app_user where open_id = #{openId}
    </select>

    <select id="selectCount"
            parameterType="com.seekisle.adminapi.appuser.domain.dto.AppUserListReqDTO"
            resultType="java.lang.Long">
        SELECT COUNT(1) FROM app_user
        <where>
            <if test="userId != null and userId != ''">
                AND id = #{userId}
            </if>
            <if test="phoneNumber != null and phoneNumber != ''">
                AND phone_number = #{phoneNumber}
            </if>
            <if test="openId != null and openId != ''">
                AND open_id = #{openId}
            </if>
            <if test="nickName != null and nickName != ''">
                AND nick_name LIKE CONCAT('%', #{nickName}, '%')
            </if>
        </where>
    </select>

    <select id="selectPage"
            parameterType="com.seekisle.adminapi.appuser.domain.dto.AppUserListReqDTO"
            resultType="com.seekisle.adminservice.user.domain.entity.AppUser">
        SELECT * FROM app_user
        <where>
            <if test="userId != null and userId != ''">
                AND id = #{userId}
            </if>
            <if test="phoneNumber != null and phoneNumber != ''">
                AND phone_number = #{phoneNumber}
            </if>
            <if test="openId != null and openId != ''">
                AND open_id = #{openId}
            </if>
            <if test="nickName != null and nickName != ''">
                AND nick_name LIKE CONCAT('%', #{nickName}, '%')
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{offset}, #{pageSize}
    </select>

</mapper>
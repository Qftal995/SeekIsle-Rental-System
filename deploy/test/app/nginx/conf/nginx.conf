worker_processes auto;
worker_cpu_affinity auto;
# 最大打开文件数，可设置为系统优化后的ulimit -HSn的结果，调整系统文件描述和这个问题有相同之处
# 需要调整系统打开文件数量
worker_rlimit_nofile 65535;

events {
    use epoll;
    worker_connections  10000;
    multi_accept on;
}

http {
    include       mime.types;
    default_type  application/octet-stream;
    sendfile        on;
    keepalive_timeout  65;
    client_max_body_size 10m;

     upstream bitegateway {
         server 192.168.100.234:18080 ;
         keepalive 1000;
     }

    # websocket咨询
    server {
            listen       81;
            server_name  "";
            client_max_body_size 100M;

            location ~* ^\/(chat\/) {
                 proxy_http_version 1.1;
                 proxy_set_header Upgrade $http_upgrade;
                 proxy_set_header Connection "Upgrade";
                 proxy_pass http://bitegateway;

                 proxy_set_header Host $host:$server_port;
                 proxy_set_header X-Real-IP $remote_addr;
                 proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

                 proxy_connect_timeout 10s;
                 proxy_read_timeout 90s;
                 proxy_send_timeout 10s;
            }
        }

    # 服务端
    server {
        listen       80;
        server_name  "";
        client_max_body_size 100M;

        location / {
            root   /data/bitehouse/web/dist;
            try_files $uri $uri/ /index.html;
            index  index.html index.htm;
	        #允许跨域请求的域，* 代表所有
		    add_header 'Access-Control-Allow-Origin' *;
		    #允许请求的header
		    add_header 'Access-Control-Allow-Headers' *;
		    #允许带上cookie请求
		    add_header 'Access-Control-Allow-Credentials' 'true';
		    #允许请求的方法，比如 GET,POST,PUT,DELETE
		    add_header 'Access-Control-Allow-Methods' *;
        }

         location ~* ^\/(admin\/|file\/|chat\/|portal\/) {
             proxy_http_version 1.1;
             proxy_set_header Connection "";
             add_header Cache-Control no-store;
             add_header Pragma  no-cache;
             proxy_set_header Host $http_host;
             proxy_set_header X-Real-IP $remote_addr;
             proxy_set_header REMOTE-HOST $remote_addr;
             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
             proxy_pass http://bitegateway;
             proxy_connect_timeout    300s;
             proxy_read_timeout       300s;
             proxy_send_timeout       300s;
         }
#
#         # 图片、视频
#         location ~ .*\.(gif|jpg|jpeg|png|bmp|swf|flv|mp4|ico)$ {
#           add_header Accept-Ranges bytes;
#           expires 7d;
#           access_log off;
#           root /data/bitshop/shop-admin/dist;
#         }
#         # 字体
#         location ~ .*\.(eot|ttf|otf|woff|svg)$ {
#           add_header Accept-Ranges bytes;
#           expires 7d;
#           access_log off;
#           root   /data/bitshop/shop-admin/dist;
#         }
#         # js、css
#         location ~ .*\.(js|css)?$ {
#           add_header Accept-Ranges bytes;
#           expires 7d;
#           access_log off;
#           root   /data/bitshop/shop-admin/dist;
#         }
#
#         # 避免actuator暴露
#         if ($request_uri ~ "/actuator") {
#             return 403;
#         }
#
#         # 禁止远程访问swagger，xrebel等
#         location ~ /api-docs {
#             return 403 FORBIDDEN!;
#         }
#         location ~ /swagger-ui.html {
#             return 403 FORBIDDEN!;
#         }
#         location ~ /rebel {
#             return 403 FORBIDDEN!;
#         }
#         location ~ /druid/ {
#             return 403 FORBIDDEN!;
#         }
#
#         error_page   500 502 503 504  /50x.html;
#         location = /50x.html {
#             root html;
#         }
    }
}